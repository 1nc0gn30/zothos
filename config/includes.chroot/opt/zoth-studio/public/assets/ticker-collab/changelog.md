# Changelog — "Constellation Ledger" polish session (The Coder)

- [P1] Fixed seamless-loop seam: replaced flex `gap` with per-item `margin-right: var(--tk-gap)` so track width is exactly 2× one set and `translateX(-50%)` loops without a half-gap jump.
- [P1] Fixed underline-accent clipping: added 9px `padding-bottom` to `.tk-marquee` so the `bottom:-8px` gradient accent survives the marquee's `overflow:hidden`.
- [P1] Added the missing bottom hairline rule to `.tk-stack` (direction.md calls for 1px rules above AND below).
- [P1] ≤480px grid edge case: `.tk-logo--dup` duplicates are now display-hidden in the wrapped grid (previously the aria-hidden set rendered as visible ghost items).
- [P1] Grid centering fix at ≤480px and reduced-motion: swapped item margins for `column-gap` on the track so centered wrapping stays optically centered.
- [P2] Micro-refined ignition: reveal transition is now a slower 600ms settle (distinct from the 300ms hover), keeping the 55ms×--i stagger capped at 600ms.
- [P2] Fixed hover lag bug: `.tk-stack.tk-inview .tk-logo` out-specified `.tk-logo:hover`, so hover inherited up to 600ms stagger delay; hover/focus rules now match specificity and reset duration+delay.
- [P3] Breakpoint hardening: `touch-action: pan-y` on the marquee at ≤720px so tap-to-pause never fights vertical page scroll; `-webkit-tap-highlight-color: transparent`; `white-space: nowrap` on `.tk-logo` so names can't wrap mid-item at narrow widths; `will-change: transform` on track (reset to auto in static grids).
- [P4] Reduced-motion grid parity: masks/padding/margins/animation all neutralized, one logo set shown, hover state still works instantly (no motion) — an equal resting state, not a fallback.
- [P5] A11y audit: added cyan `:focus-visible` outline (offset 6px) for keyboard users; raised `.tk-name` muted contrast from rgba(255,255,255,.55)→.62 (~5.9:1 on dark) including reduced-motion block; focus-visible gets full-color treatment same as hover.
- [P6] JS: added focusin/focusout pause (keyboard parity — strip holds still while a logo is focused); still only class toggles, no rAF, observer untouched. `node --check` passes; all 6 referenced `/assets/logos/*.svg` verified present. CSS 6.1KB / JS 1.5KB.
- [P7] Cross-browser: forced a compositing layer on `.tk-track` (`translateZ(0)`, carried into the keyframes) to kill Safari's masked-track repaint flicker per loop; made `animation-timing-function: linear` explicit on the track so Firefox never inherits an eased timing; reset `transform` alongside `animation` in the ≤480px and reduced-motion grid states.
- [P8] Micro-typography: `.tk-name` tracking tuned per breakpoint (0.08em → 0.06em ≤720px → 0.05em ≤480px/reduced-motion, tighter at smaller mono sizes); logos optically aligned to the text axis with a half-pixel lift on `img`; per-logo optical-weight normalization added (§5): Grok +2px, Cerebras +3px, DeepSeek +1px rendered height — their glyphs under-fill the shared 100×100 viewBox and read small; applied via new `tk-logo--*` classes in snippet.html, both sets, img-only so hover transform untouched; alignment lift preserved in reduced-motion (it's alignment, not motion).
- [P9] Interaction edge cases: pause state re-centered on one `apply()` source of truth with a `stickyPaused` intent flag — touch tap toggle survives synthesized mouse events on hybrid devices; mouseleave can't race a focusin into unpausing a focused strip; focusout defers a tick so tabbing between adjacent logos never flickers; explicit visibilitychange pause/resume restores exactly the pre-hide state (browsers throttle CSS animations inconsistently when hidden). Still class toggles only, no rAF.
- [P10] Code quality: CSS reorganized into 7 numbered comment sections (tokens/frame, label row, marquee, logos, optical normalization, breakpoints, reduced motion); deduped repeated hairline color into `--tk-line`; JS given matching numbered sections and a documented pause-state contract. `node --check` passes. CSS 7.9KB / JS 3.0KB.
- [P11] Real-render verification: added standalone `preview.html` (tokens stand-in + verbatim snippet + css/js links); node check confirms all 8 referenced `/assets/...` paths resolve on disk; `node --check` passes.
- [P12] Drop-in stress test (scripted): every class in snippet.html exists in ticker.css, every id/class the JS queries exists in markup, zero true orphan selectors (`tk-inview`/`tk-paused` are JS-applied state classes, documented as such).
- [P13] Timing feel pass: track ≈ 2×1303px ≈ 2606px → 46s loop = ~57px/s desktop (~35px/s mobile at 60s). That's glacial but alive — a logo set crosses a viewport in ~17s, well inside marquee norms — so 46s stands, no change.
- [P14] Wrote README.md: what it is, design story (vision→direction→spec), exact integration steps for zoth/index.html (replace `.sovereign-stack` block, add link+deferred script), and the don'ts list.
- [P15] preview.html realism upgrade: replaced stand-in tokens with the verbatim `:root` block from zoth/index.html (--gold #e8c872, --cyan, --line rgba(247,244,238,.1), full --fib-* scale, IBM Plex Mono/Syne/Figtree) + the site's Google Fonts load; body now uses var(--bg)/var(--text)/var(--font-sans). Preview renders exactly like production.
- [P16] Hover micro-feel A/B in preview only: `?hover=b` sets `data-hover-b` on `<html>`, overriding `--tk-ease` to a springier cubic-bezier(0.34, 1.3, 0.64, 1); default stays the production curve (0.22, 1, 0.36, 1). ticker.css untouched; README documents flipping the winner into §1's `--tk-ease` token.
- [P17] Logo strip content polish: mono labels verified consistent (Title Case in markup, uniform uppercase via CSS `text-transform`, identical spacing/structure across all 12 items); anchors given meaningful aria-labels ("\<Name\> — supported model provider", 6+6 sets, snippet + preview kept verbatim-parity); text-only mode confirmed — every logo item carries its `.tk-name` span so the row reads perfectly with images disabled.
- [P18] Final regression sweep PASS: `node --check` ticker.js OK; all 8 `/assets/...` refs resolve on disk; every snippet class exists in ticker.css (state classes tk-inview/tk-paused JS-applied as documented); all JS-queried ids/classes present in markup; keyframes animate transform-only, zero layout-property animations; preview snippet matches snippet.html byte-for-byte. PASS
- [P19] Dark-room test: against production tokens (--line rgba(247,244,238,.1), --mute #a8a4c2) the hairlines and label read as quiet structure that SETTLES the page after the bright gold deck rather than competing — border/label untouched, judgment call logged, no change.
- [P20] Motion signature audit: 46s loop = ~57px/s; even the longest item ("NOUS RESEARCH", ~200px) holds ~24s of visibility crossing a 1200px viewport — 10× past the 4s readability floor — so 46s stands, no slowdown to 52s.
- [P21] No-JS degradation FIXED (was a real bug): pre-ignition hide is now gated on `.tk-armed`, added by ticker.js before observing — if the script fails/blocks, logos render visible at rest and ignition can only enhance; also fixed malformed `class="… aria-label="` attribute quoting on all 12 anchor tags in snippet.html (+ preview kept verbatim-parity).
- [P22] Craft pass: read ticker.css line-by-line — every rule traces to a documented decision (compositing layer, mask padding, specificity pairings); nothing clever-but-unneeded found, zero removals; `node --check` ticker.js PASS.
- P23 (hermes review): fixed malformed attribute quoting left by P21 aria pass — `aria-label="..."> style=` broke HTML parsing in all 12 anchor tags across snippet.html + preview.html
- [P24] Integration rehearsal: preview.html now wraps the verbatim snippet in a faithful replica of the real context — section.fib-section#alchemical-doctrine (fib-89/pad padding, max-width 1400px) > .alchemical-deck golden-split grid card > left min-width:0 column with production-like content mass above the ticker. Catches what the old 960px frame couldn't: the ticker's real desktop home is a ~55% grid column (~600px), not a full-width strip. Verbatim-parity re-verified byte-for-byte; snippet untouched.
- [P25] Performance instrumentation (preview-only, dev-only): console.time('tk:init') from parse to tk-armed class landing (<5ms PASS/FAIL line) + MutationObserver counting tk-inview applications (must be exactly once). ticker.js/ticker.css/snippet.html untouched; node --check PASS.
- [P26] Documentation final pass: README gains 'Design Decisions' appendix table (decision / rationale / pass number): 46s duration, --i stagger cap 600ms, tk-armed gating, translateY(-1px) max, margin-vs-gap seam, linear+translateZ track, optical normalization, sticky pause, deferred focusout, reduced-motion grid, contrast raise, hairline frame. File table updated to passes 1–27.
- [P27] Zen pass: re-read direction.md north star; full fresh-eyes review of component in its new narrow-column rehearsal context — resting 45% grayscale, hairlines at 0.1 alpha, single 7px gold dot (vision-mandated punctuation), one-shot ignition, earned-only color. Nothing draws attention before the logos; in the narrower column the drift is effectively slower still. RESTRAINT IS THE FINAL CHANGE.
- [P28] Fallback audit: every SITE var() (--line, --fib-*, --font-mono, --mute, --gold, --cyan, --i) already carries an inline fallback — component survives paste into a page without site tokens; bare refs are component-owned tokens defined on .tk-stack itself; self-containment contract documented in ticker.css §1.
- [P29] Content-variant robustness derived: track width is always exactly 2× one set regardless of item count (4/8) or name length ("Mistral AI", "Cohere") — translateX(-50%) holds as long as the duplicate half mirrors originals in content, order, and modifier classes; seam constraint documented at .tk-marquee in §3 (8 items ⇒ ~74px/s at fixed 46s, still inside norms).
- [P30] Light-context guard: minimal @media (prefers-color-scheme: light) re-inks ONLY --tk-line and .tk-label color (the two things that go unreadable on a white page); logos/names/accents deliberately untouched — choice logged in ticker.css §8.
- [P31] Final packaging: snippet.html header now states what it is, both wire-in tags, the block it replaces (.sovereign-stack in /public/zoth/index.html), and points to README.md; byte-parity between snippet.html markup and preview.html's verbatim copy re-verified (2883 = 2883 bytes). node --check PASS.
- [P32] Animation-composition audit: ignition vs hover overlap test-reasoned — hover wins on source order (equal specificity) so mid-ignition hover is instant 300ms/0ms (no jank); un-hover mid-ignition re-applies the ≤600ms stagger once, judged acceptable and documented in ticker.css §4; track keyframes touch transform only, disjoint from all logo transitions. No code change needed.
- [P33] Focus-ring placement fixed: :focus-visible outline (1px + 6px offset = 7px) clipped at .tk-marquee's top edge under overflow:hidden — added symmetric padding-top: 8px (zeroed alongside padding-bottom in the ≤480px and reduced-motion grid states); horizontal clipping at the marquee viewport edges accepted as inherent + mask-faded.
- [P34] Reduced-motion × light-scheme matrix verified: the two @media blocks are independent — reduced-motion never redefines --tk-line or .tk-label color, so the light guard's hairline/label re-ink applies cleanly in the static grid too; no interaction conflict. PASS, no change.
- [P35] Minify-readiness: sources stay unminified/readable by design; README gains a Minification section noting a ticker.min.css/js pair can be generated at integration time (lightningcss/terser), regenerate-don't-edit. No minification performed.
- [P36] Print stylesheet: new §9 @media print collapses to one static row (animation/masks/ignition states neutralized, duplicates hidden, names inked black, break-inside avoid) — a printed page shows a clean logo row.
- [P37] High-DPI sanity PASS: all 6 SVGs are viewBox-only (100×100, no width/height attrs) so they rasterize crisp at any DPR; snippet img tags carry no fixed width attributes — nothing conflicts with the CSS height sizing.
- [P38] README gains copy-paste-ready 'INTEGRATION CHECKLIST': ordered steps (replace .sovereign-stack block in zoth/index.html ~1153–1170, add ticker.css link, add deferred ticker.js) + 9 post-integration smoke tests.
- [P39] Sign-off: full re-read of all files; node --check PASS; byte-parity snippet↔preview 2882=2882 PASS; all 6 logo SVGs viewBox-only (crisp at any DPR), no fixed width attrs anywhere; SIGNOFF.md written — v1.0.0, passes 1–39, 4 documented limitations, verdict SHIP.
