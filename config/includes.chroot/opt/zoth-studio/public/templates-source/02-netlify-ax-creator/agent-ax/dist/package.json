{
  "name": "agent-ax",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "netlify dev",
    "build": "mkdir -p dist && cp -R public dist/public && cp -R netlify/functions dist/functions && cp netlify.toml dist/netlify.toml && cp package.json dist/package.json",
    "deploy": "netlify deploy --prod"
  },
  "dependencies": {
    "better-sqlite3": "^11.0.0"
  },
  "devDependencies": {
    "@netlify/cli": "^21.0.0"
  }
}
